---

models:
  - {model: {{constraints['model']}}, project: CMIP5,  mip: {{constraints['cmor_table']}},  exp: {{constraints['experiment']}},  ensemble: {{constraints['ensemble']}},  start_year: {{start_year}},  end_year: {{end_year}}}
  - {model: ERA-Interim, project: OBS, tier: 3, type: reanaly, version: 1, start_year: {{ start_year }}, end_year: {{ end_year }}}

preprocessors:
  preprocessor_1:
    extract_levels:
      levels: 85000
      scheme: nearest
    regrid:
      target_grid: ERA-Interim
      scheme: linear

diagnostics:
  ta_diagnostic:
    description: Air temperature Python tutorial diagnostics.
    variables:
      ta:
        preprocessor: preprocessor_1
        field: T3M
    scripts:
      test_ta:
        script: example_diagnostic.py
        title: "Python example diagnostic"
